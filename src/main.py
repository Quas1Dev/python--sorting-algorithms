# Import the insertionSort function from insertion-sort.py
from insertion_sort import insertionSort

sequence = [1,3,4,2,5,0]


orderedSequence = insertionSort(sequence)

print(f"Ordered sequence: {orderedSequence}")